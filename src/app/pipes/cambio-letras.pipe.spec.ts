import { CambioLetrasPipe } from './cambio-letras.pipe';

describe('CambioLetrasPipe', () => {
  it('create an instance', () => {
    const pipe = new CambioLetrasPipe();
    expect(pipe).toBeTruthy();
  });
});
