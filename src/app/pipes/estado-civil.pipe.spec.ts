import { EstadoCivilPipe } from './estado-civil.pipe';

describe('EstadoCivilPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoCivilPipe();
    expect(pipe).toBeTruthy();
  });
});
